{
  "name": "art-portfolio-api-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --grep-invert @admin --reporter=list,html",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.16.10",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.2"
  }
}
